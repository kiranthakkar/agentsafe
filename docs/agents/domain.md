# Domain docs

This is a single-context repository. Engineering workflows should read root
`CONTEXT.md` and relevant `docs/adr/` files when they exist; their absence is
not an error. Use terminology defined by `CONTEXT.md` and flag any conflict
with an ADR rather than silently overriding it.
