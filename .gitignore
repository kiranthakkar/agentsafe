.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/

# Local ad-hoc testing artifacts in the repo root only. examples/ intentionally
# commits its own appconfig/.env demo — see examples/README.md.
/appconfig
/.agentsafe/
/.env

# Lock files and .env.agent are never safe or useful to commit, anywhere.
appconfig.lock
.agentsafe/*.lock
.env.lock
.env.agent
.env.agent.lock
